{
  "name": "betoken",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ ZeframLou/betoken",
    "create-local": "graph create --node http://localhost:8020/ ZeframLou/betoken",
    "remove-local": "graph remove --node http://localhost:8020/ ZeframLou/betoken",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ZeframLou/betoken"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.16.0",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
